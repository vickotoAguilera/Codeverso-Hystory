rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas temporales para desarrollo (Válidas hasta 2026-06-09)
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 9);
    }
  }
}
